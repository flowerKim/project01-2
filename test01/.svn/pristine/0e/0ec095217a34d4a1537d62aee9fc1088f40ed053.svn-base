package net.bitacademy.java72.service;

import java.util.List;

import net.bitacademy.java72.domain.Repl;

public interface ReplService {
  List<Repl> list(int pageNo, int pageSize, int no); 
  int insert(Repl repl);
  int update(Repl repl);
  int delete(int no);
  Repl get(int no);
  Repl getView(int no);
  int updateView(Repl repl);
  int countAll();
  
}
